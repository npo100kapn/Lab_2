        function Array()
        {
            leta = [];
            for (i=0; i<arguments.length; i++)
            a.push (arguments [i]);
            return (a);
        }

        Array (1,3,5,8,7);