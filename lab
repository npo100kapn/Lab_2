function map(array, fn) {
	let res = [];
	for(let i = 0; i< array.length;i++){
		item = fn (array[i]);
		res.push(item);
	}
	console.log (res);
}

function funcx (a){
	return (a.toUpperCase());
}

let arr = ["Hello", "word"];
map(arr, funcx);

Array (1,3,5,8,7);